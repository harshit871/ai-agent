[AI_AGENT]
name = "ai-agent"
version = "0.1.0"
description = "A minimal AI coding agent powered by Google’s Gemini API — inspired by Claude Code, built for experimentation and learning."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
